# 路线拆分记录

时间：2026-03-30 19:22:29

## 1. 背景

在 P1 执行节点已进入 GitHub Tracker 后，需要把 L0 层的后续阶段也放入同一张路线图中，避免路线图只覆盖当前活动切片。

## 2. 本次拆分结果

1. `P2` 对应 Issue `#5`
2. `P3` 对应 Issue `#4`
3. `P4` 对应 Issue `#6`

## 3. 同步规则

1. 当前活动执行仍以 `P1` 为主
2. `P2/P3/P4` 作为路线图层级条目，当前状态保持 `Todo`
3. 工作类型暂标记为 `Plan`
4. 待上游阶段稳定后，再继续向更细执行契约拆分
