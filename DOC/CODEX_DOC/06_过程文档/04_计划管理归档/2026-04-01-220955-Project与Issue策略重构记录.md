# Project 与 Issue 策略重构记录

时间：2026-04-01 22:09:55

## 1. 背景

用户要求基于现有工程研发策略，重构 GitHub Project 与 Issue，包括但不限于：

- WBS 节点树形结构可视化
- Issue 内容设置标准化

## 2. 执行动作

1. Project 增补字段：
   - `WBS Node`（文本）
   - `Parent Node`（文本）

2. Project 回填字段：
   - #1~#17 的 `WBS Node/Parent Node` 全量回填

3. Issue 正文标准化：
   - #1~#17 增加 `WBS 节点` 段
   - #9~#17 的 `Depends On` 和 `上级节点` 统一为 `#Issue编号` 引用
   - #7 上级节点统一为 `ROOT`

4. 仓库模板固化：
   - 新增 `.github/ISSUE_TEMPLATE/00-wbs-parent-node.md`
   - 新增 `.github/ISSUE_TEMPLATE/01-wbs-execution-node.md`
   - 新增 `.github/ISSUE_TEMPLATE/config.yml`

5. 策略文档同步：
   - 更新本地工作策略和开发计划入口文档，记录字段基线和权限边界

## 3. 权限边界

- 无 `AddSubIssue` 权限，未建立 GitHub 原生父子 Issue
- 无标签维护权限，未建立 label 治理体系

## 4. 当前结果

- 已形成可扫描的 WBS 树表达：
  - Project 字段表达：`WBS Level/WBS Node/Parent Node`
  - Issue 契约表达：`WBS 节点/上级节点/子节点索引`
- 状态：`待人工验收`
