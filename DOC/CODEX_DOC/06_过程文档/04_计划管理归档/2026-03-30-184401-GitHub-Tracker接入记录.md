# GitHub Tracker 接入记录

时间：2026-03-30 18:44:01

## 1. 背景

NodeTest 已形成稳定的本地设计稿、WBS 计划和执行契约结构，需要把当前活动计划同步到可扫描的外部 Tracker。

## 2. 本次决策

1. GitHub Project 作为 Tracker 层
2. GitHub Issue 作为执行契约层
3. 本地 `DOC/CODEX_DOC/` 作为长文档层

## 3. 权限约束

当前活跃账号为 `wgwtest`。

已确认：

- `wgwtest` 对仓库 `hugowangguowei/NodeTest` 的 `viewerPermission` 为 `READ`
- `wgwtest` 具备 `project` scope

因此采用：

1. Project 建在 `wgwtest`
2. Issue 建在 `hugowangguowei/NodeTest`

## 4. 本次建立的线上对象

### 4.1 Project

- `NodeTest Delivery Roadmap`
- `https://github.com/users/wgwtest/projects/2`

### 4.2 Issue

1. `#1 [P1.1] 文档与策略面稳定`
2. `#2 [P1.2] 平台定义面稳定`
3. `#3 [P1.3] 浏览器原型人工验收与反馈闭环`

## 5. Project 字段基线

1. `Status`
2. `Phase`
3. `Contributes To`
4. `Work Type`
5. `Start Date`
6. `Target Date`

## 6. 说明

这份记录属于计划管理过程留痕，不作为当前执行入口。当前执行入口仍以本地计划 README、WBS 当前节点和对应 Issue / Project 为准。
