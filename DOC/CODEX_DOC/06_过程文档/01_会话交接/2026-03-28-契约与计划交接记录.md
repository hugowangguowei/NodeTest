# 2026-03-28 契约与计划交接记录

## 1. 本轮目标

按顺序完成以下两项工作：

1. 收敛四层核心模型的正式契约
2. 基于已确认设计稿输出实现计划

## 2. 已完成内容

### 2.1 设计基线

已形成并保留以下基线文档：

- `2026-03-28-项目理解与待确认问题.md`
- `00-平台总设计稿.md`

### 2.2 正式契约草案

已新增：

- `2026-03-28-四层核心标准规范草案.md`

该文档明确了：

- `Standard Knowledge`
- `Scenario`
- `Experience`
- `Build Workflow`

四层对象边界、最小字段、引用规则与旧样本过渡关系。

### 2.3 实现计划

已新增：

- `2026-03-28-契约优先原型实现计划.md`

该计划覆盖：

- Node.js 原型运行骨架
- 四层 schema 与 Ajv 校验
- 场景组合服务
- 表达投影服务
- 构建流程记录
- 最小 HTTP API
- 自测与交接文档补充

## 3. 当前状态

- 当前仓库仍处于“文档和计划已收敛，代码尚未开始”的阶段
- 文档根已固定为 `DOC/CODEX_DOC/`
- 由于本机未配置 git 身份信息，本轮未能提交 commit

## 4. 建议下一步

优先顺序如下：

1. 用户 review 契约草案与实现计划
2. 选择执行方式：
   - 子代理分任务执行
   - 当前会话内联执行
3. 进入计划执行前，补齐本机 git `user.name` 与 `user.email`

## 5. 风险提醒

- 当前旧样本 `Data/Knowledge_sample.json` 仍是过渡结构，后续实现时不能直接当正式契约
- 如果实现时重新混用 `flow`、`business_flow`、`build_workflow`，会重新引入模型漂移
- 在 UI 尚未实现前，不要让页面概念提前侵入 `Scenario` 或 `Experience` 的核心定义
