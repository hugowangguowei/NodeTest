# 人工验收清单 - Issue 路径相对化与策略同步

时间：2026-04-02 12:57:22

## 1. 验收目标

确认 NodeTest 的 GitHub 协作面已统一采用仓库相对路径，且策略与模板已固化该规则。

## 2. 验收步骤

1. 打开任意 3 个执行 Issue（建议 `#1/#8/#16`）
2. 检查以下字段：
   - `验收入口`
   - `输出物`
   - `证据链接`
3. 确认字段中的文件路径为仓库相对路径（如 `DOC/...`、`src/...`）
4. 确认不再出现 `/home/wgw/...` 绝对路径
5. 打开模板文件：
   - `.github/ISSUE_TEMPLATE/00-wbs-parent-node.md`
   - `.github/ISSUE_TEMPLATE/01-wbs-execution-node.md`
6. 确认模板中存在“路径规范”条款：
   - 相对路径优先
   - 可选仓库 URL
   - 禁止本机绝对路径
7. 打开本地策略文档：
   - `DOC/CODEX_DOC/00-本地工程策略映射.md`
   - `.github/ISSUE_TEMPLATE/00-wbs-parent-node.md`
   - `.github/ISSUE_TEMPLATE/01-wbs-execution-node.md`
8. 确认策略文档包含同口径规则

## 3. 通过标准

- [ ] Issue 路径全部为仓库相对路径或仓库 URL
- [ ] Issue 中无 `/home/wgw/...` 绝对路径
- [ ] 两份 Issue 模板均包含路径规范
- [ ] 本地策略与 Issue 模板已同步路径规范
