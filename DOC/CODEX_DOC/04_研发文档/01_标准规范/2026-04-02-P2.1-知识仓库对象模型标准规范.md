# 2026-04-03 P2.1 知识仓库对象模型标准规范

## 1. 文档定位

本稿是 P2.1 的对象模型标准规范，用于为后续 P2.2/P2.3/P2.4 提供统一结构基线。

本稿先定义“知识仓库本体（聚合根）”，再定义状态域与对象字段规范。状态域属于仓库组成部分，不替代仓库本体。

### 1.1 建模依据（通用样式）

1. 领域驱动设计（DDD）聚合根思想：将 `knowledge_repository` 作为统一边界，所有对象通过 `repository_ref` 归属同一聚合。
2. JSON Schema 契约化建模：字段语义与结构约束分层，文档负责解释语义，schema 负责运行时校验。
3. 流程建模通用拆分：流程定义（`business_flow`）与节点（`flow_node`）分离，并通过事件/规则关联。
4. 审计追溯通用模式：运行态与审计态分层，`audit_trace` 域采用只增不改策略。
5. 元数据分层实践：标识字段与扩展元信息分离，避免核心主键语义与描述语义耦合。

### 1.2 命名分层原则

1. `repository_*` 前缀保留给仓库一级固定字段（标识、名称、版本）。
2. `meta.*` 仅承载扩展元信息，不承载仓库主标识字段。
3. 历史材料若存在 `meta.name`，在本规范中统一收敛为 `repository_name`。

## 2. 阅读与对照说明

1. 本文档负责“字段语义解释 + 约束规则”。
2. `src/contracts/standardKnowledge.schema.json` 负责运行时校验（以当前工程可执行输入为主）。
3. 为便于人工评阅与字段对照，新增样例文件：
   - `DOC/CODEX_DOC/04_研发文档/01_标准规范/2026-04-03-P2.1-知识仓库对象模型样例数据.json`

## 3. 顶层仓库聚合根规范

### 3.1 结构样例

```json
{
  "knowledge_repository": {
    "repository_id": "repo-001",
    "repository_name": "跨领域知识仓库",
    "repository_version": "2.1.0-draft",
    "meta": {
      "owner": "platform-team",
      "taxonomy_version": "v1",
      "description": "跨领域复用的标准知识仓库"
    },
    "scope": {
      "domain_scope": "cross-domain",
      "tenant_mode": "single-tenant"
    },
    "domain_catalog": {
      "state_domains": []
    },
    "object_catalog": {
      "semantic_objects": {
        "entities": [],
        "behaviors": [],
        "events": [],
        "relationships": [],
        "business_flows": [],
        "rules": []
      },
      "runtime_objects": {
        "flow_instances": [],
        "node_instances": []
      },
      "audit_objects": {
        "operation_logs": [],
        "state_snapshots": []
      }
    },
    "indexes": {
      "id_index": {},
      "type_index": {},
      "domain_index": {}
    },
    "writeback_policy": {
      "mode": "append_with_validation",
      "conflict_strategy": "reject_and_report"
    },
    "audit_policy": {
      "retention": "long_term",
      "mutable": false
    }
  }
}
```

### 3.2 字段说明

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `knowledge_repository` | object | 是 | 仓库聚合根，所有对象必须归属此根 |
| `repository_id` | string | 是 | 仓库唯一标识，跨模块引用主键 |
| `repository_name` | string | 是 | 仓库业务名称（面向阅读与管理） |
| `repository_version` | string | 是 | 仓库结构版本，用于升级与兼容 |
| `meta` | object | 是 | 仓库扩展元信息 |
| `meta.owner` | string | 是 | 维护责任方或团队 |
| `meta.taxonomy_version` | string | 是 | 领域分类体系版本 |
| `meta.description` | string | 否 | 仓库用途描述（可选） |
| `scope.domain_scope` | string | 是 | 领域范围，例如 `cross-domain` |
| `scope.tenant_mode` | string | 是 | 租户模式，示例 `single-tenant` |
| `domain_catalog.state_domains` | array | 是 | 状态域注册清单 |
| `object_catalog.semantic_objects` | object | 是 | 语义层对象集合 |
| `object_catalog.runtime_objects` | object | 是 | 运行时对象集合 |
| `object_catalog.audit_objects` | object | 是 | 审计对象集合 |
| `indexes` | object | 是 | 索引集合（定位/校验/去重） |
| `writeback_policy` | object | 是 | 回写策略（时序、冲突） |
| `audit_policy` | object | 是 | 审计策略（保留与可变性） |

### 3.3 约束规则

1. `knowledge_repository` 是唯一聚合根，所有对象必须可追溯到该根。
2. `domain_catalog.state_domains` 是状态域注册清单，业务对象只能引用已注册域。
3. `object_catalog` 用于对象托管与生命周期管理，不允许对象游离于目录外。
4. `indexes` 是仓库级索引，不等同于业务对象字段。
5. `repository_name` 属于仓库一级显式字段，不再使用 `meta.name` 作为仓库主名称。

## 4. 状态域对象规范（仓库组成）

### 4.1 结构样例

```json
{
  "id": "state-domain-001",
  "repository_ref": "repo-001",
  "name": "知识语义域",
  "domain_type": "knowledge_semantic",
  "description": "承载实体、行为、事件、关系、流程、规则",
  "object_refs": [],
  "update_policy": "versioned_upsert"
}
```

### 4.2 字段说明

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `id` | string | 是 | 状态域唯一标识 |
| `repository_ref` | string | 是 | 归属仓库引用 |
| `name` | string | 是 | 状态域名称 |
| `domain_type` | string | 是 | 状态域类型（受控枚举） |
| `description` | string | 否 | 状态域用途说明 |
| `object_refs` | array[string] | 是 | 域内已注册对象引用 |
| `update_policy` | string | 是 | 域级更新策略 |

### 4.3 约束规则

1. `domain_type` 必须来自受控枚举：
   - `master_data`
   - `knowledge_semantic`
   - `process_runtime`
   - `projection_state`
   - `audit_trace`
2. `repository_ref` 必须指向已存在仓库聚合根。
3. `object_refs` 仅允许引用 `object_catalog` 已注册对象。

## 5. 语义对象规范

### 5.1 通用字段（所有语义对象共用）

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `id` | string | 是 | 对象唯一标识 |
| `repository_ref` | string | 是 | 对象归属仓库 |
| `name` | string | 是 | 对象业务名称 |
| `state_domain_ref` | string | 是 | 对象所在状态域 |

### 5.2 `domain_entity`

```json
{
  "id": "entity-001",
  "repository_ref": "repo-001",
  "name": "事项申请单",
  "entity_type": "business_object",
  "attribute_schema_ref": "attr-schema-001",
  "state_domain_ref": "state-domain-001",
  "source_refs": [],
  "status": "active"
}
```

补充字段说明：

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `entity_type` | string | 是 | 实体类型 |
| `attribute_schema_ref` | string | 否 | 实体属性结构定义引用 |
| `source_refs` | array[string] | 否 | 来源证据引用 |
| `status` | string | 是 | 生命周期状态 |

角色与人员建模约定：

1. “董事长/总经理/财务主管/合规主管/执行人员”等角色，在本规范中作为 `domain_entity` 建模，`entity_type` 建议使用 `person_role`。
2. 流程中的执行主体统一通过 `behavior.actor_refs` 和 `operation_log.operator_ref` 引用这些角色实体。
3. 需要区分“岗位角色”和“具体人员”时，可继续扩展 `entity_type=person_actor`，并通过 `domain_relationship` 维护 `person_actor -> person_role` 关系。

角色实体样例：

```json
{
  "id": "entity-role-002",
  "repository_ref": "repo-001",
  "name": "总经理",
  "entity_type": "person_role",
  "state_domain_ref": "state-domain-001",
  "source_refs": [],
  "status": "active"
}
```

### 5.3 `domain_behavior`

```json
{
  "id": "behavior-001",
  "repository_ref": "repo-001",
  "name": "提交申请",
  "behavior_type": "action",
  "actor_refs": ["entity-role-005"],
  "target_refs": ["entity-001"],
  "rule_refs": ["rule-001"],
  "state_domain_ref": "state-domain-001"
}
```

补充字段说明：

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `behavior_type` | string | 是 | 行为类型 |
| `actor_refs` | array[string] | 否 | 行为主体引用 |
| `target_refs` | array[string] | 否 | 行为目标引用 |
| `rule_refs` | array[string] | 否 | 行为关联规则引用 |

### 5.4 `domain_event`

```json
{
  "id": "event-001",
  "repository_ref": "repo-001",
  "name": "申请已提交",
  "event_type": "business_event",
  "trigger_behavior_ref": "behavior-001",
  "state_change_refs": ["state-change-001"],
  "state_domain_ref": "state-domain-002"
}
```

补充字段说明：

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `event_type` | string | 是 | 事件类型 |
| `trigger_behavior_ref` | string | 否 | 触发该事件的行为 |
| `state_change_refs` | array[string] | 否 | 事件引起的状态变化引用 |

### 5.5 `domain_relationship`

```json
{
  "id": "relation-001",
  "repository_ref": "repo-001",
  "name": "事项归属部门",
  "relationship_type": "belongs_to",
  "from_ref": "entity-001",
  "to_ref": "entity-group-001",
  "rule_refs": [],
  "state_domain_ref": "state-domain-001"
}
```

补充字段说明：

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `relationship_type` | string | 是 | 关系语义类型 |
| `from_ref` | string | 是 | 关系起点 |
| `to_ref` | string | 是 | 关系终点 |
| `rule_refs` | array[string] | 否 | 关系约束规则引用 |

### 5.6 `business_flow` 与 `flow_node`

`business_flow` 样例：

```json
{
  "id": "business-flow-001",
  "repository_ref": "repo-001",
  "name": "事项审批流程",
  "flow_type": "approval_flow",
  "node_refs": ["flow-node-001", "flow-node-002"],
  "entry_rule_refs": [],
  "exit_rule_refs": [],
  "state_domain_ref": "state-domain-001"
}
```

`flow_node` 样例：

```json
{
  "id": "flow-node-001",
  "repository_ref": "repo-001",
  "name": "受理",
  "node_type": "action_node",
  "behavior_refs": ["behavior-001"],
  "event_refs": ["event-001"],
  "next_node_refs": ["flow-node-002"]
}
```

补充字段说明：

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `flow_type` | string | 是 | 流程类型 |
| `node_refs` | array[string] | 是 | 流程节点列表 |
| `entry_rule_refs` | array[string] | 否 | 进入流程前置规则 |
| `exit_rule_refs` | array[string] | 否 | 退出流程后置规则 |
| `node_type` | string | 是 | 节点类型 |
| `behavior_refs` | array[string] | 否 | 节点绑定行为 |
| `event_refs` | array[string] | 否 | 节点触发/消费事件 |
| `next_node_refs` | array[string] | 否 | 后继节点引用 |

### 5.7 `domain_rule`

```json
{
  "id": "rule-001",
  "repository_ref": "repo-001",
  "name": "材料完整性规则",
  "rule_type": "precondition",
  "scope_refs": ["flow-node-001"],
  "expression": "materials.complete == true",
  "state_domain_ref": "state-domain-001"
}
```

补充字段说明：

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `rule_type` | string | 是 | 规则类型 |
| `scope_refs` | array[string] | 是 | 规则作用范围 |
| `expression` | string | 是 | 规则表达式 |

## 6. 运行与审计对象规范

### 6.1 `flow_instance`

```json
{
  "id": "flow-inst-001",
  "repository_ref": "repo-001",
  "flow_ref": "business-flow-001",
  "current_node_ref": "flow-node-001",
  "status": "running",
  "started_at": "2026-04-02T00:00:00Z",
  "state_domain_ref": "state-domain-003"
}
```

### 6.2 `node_instance`

```json
{
  "id": "node-inst-001",
  "repository_ref": "repo-001",
  "node_ref": "flow-node-001",
  "flow_instance_ref": "flow-inst-001",
  "status": "completed",
  "completed_at": "2026-04-02T00:10:00Z",
  "state_domain_ref": "state-domain-003"
}
```

### 6.3 `operation_log`

```json
{
  "id": "oplog-001",
  "repository_ref": "repo-001",
  "operation_type": "state_update",
  "operator_ref": "entity-role-003",
  "target_ref": "flow-inst-001",
  "before_snapshot_ref": "snapshot-001",
  "after_snapshot_ref": "snapshot-002",
  "created_at": "2026-04-02T00:10:00Z",
  "state_domain_ref": "state-domain-005"
}
```

### 6.4 `state_snapshot`

```json
{
  "id": "snapshot-001",
  "repository_ref": "repo-001",
  "target_ref": "flow-inst-001",
  "payload": {},
  "captured_at": "2026-04-02T00:09:59Z",
  "state_domain_ref": "state-domain-005"
}
```

运行与审计关键字段说明：

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `flow_ref` | string | 是 | 实例对应的流程定义 |
| `current_node_ref` | string | 否 | 当前节点 |
| `flow_instance_ref` | string | 是 | 节点实例归属流程实例 |
| `operation_type` | string | 是 | 操作类型 |
| `before_snapshot_ref` | string | 否 | 变更前快照 |
| `after_snapshot_ref` | string | 否 | 变更后快照 |
| `payload` | object | 是 | 快照实体内容 |

## 7. 样例数据与对照

1. 同目录样例数据：
   - `DOC/CODEX_DOC/04_研发文档/01_标准规范/2026-04-03-P2.1-知识仓库对象模型样例数据.json`
2. 运行时校验规范：
   - `src/contracts/standardKnowledge.schema.json`
3. 校验样例（自动化测试使用）：
   - `tests/fixtures/standardKnowledge.valid.json`

说明：本目录样例用于“人工评审与字段对照”；`src/contracts` 用于“程序校验和接口执行”。

## 8. 通用规范规则

1. 所有对象必须归属 `knowledge_repository`，并具备 `repository_ref`。
2. 所有跨对象关联必须使用 `id`/`*_ref`/`*_refs`。
3. 所有业务对象必须带 `state_domain_ref`，禁止无域对象。
4. `audit_trace` 域对象只增不改，禁止覆盖历史日志。
5. 不允许使用页面术语（例如“左栏卡片”“tab 状态”）作为正式对象字段。

## 9. 后续节点输入

1. 输入给 P2.2：异构对象编排规范（对象如何被工厂编排）。
2. 输入给 P2.3：软件构建工作流规范与流程定义（工厂编排与执行流程）。
3. 输入给 P2.4：仓库统一回写链路与一致性校验（回写时序与冲突策略）。

## 10. 参考资料

1. Eric Evans, *Domain-Driven Design: Tackling Complexity in the Heart of Software*（聚合根与领域对象边界）。
2. JSON Schema Draft 2020-12（对象结构与约束表达，本项目 `src/contracts` 采用该版本）。
3. BPMN 2.0（流程、节点、事件拆分的通用流程建模参考）。
4. Martin Fowler, *Event Sourcing*（运行态与审计态分离、追加式审计日志思路）。
