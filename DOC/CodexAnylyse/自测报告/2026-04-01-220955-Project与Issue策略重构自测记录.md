# Project 与 Issue 策略重构自测记录

时间：2026-04-01 22:09:55
执行人：Codex

## 1. 自测范围

- GitHub Project 字段与条目值校验
- GitHub Issue 契约结构校验
- 本地策略文档与模板文件校验

## 2. 执行记录

1. Project 字段校验：
   - 命令：`gh project field-list 2 --owner wgwtest`
   - 结果：存在 `WBS Level`、`WBS Node`、`Parent Node`

2. Project 条目值校验：
   - 命令：`gh project item-list 2 --owner wgwtest --limit 100 --format json`
   - 结果：#1~#17 的 `WBS Level/WBS Node/Parent Node` 均有值

3. Issue 契约段校验：
   - 命令：`gh issue list --json number,body` + `jq` 检查必填段
   - 结果：#1~#17 必填段全部通过

4. 依赖与父节点编号化校验：
   - 命令：`gh issue list --json number,body` + `jq` 抽取 `Depends On/上级节点`
   - 结果：#9~#17 均为 `#编号` 引用，#7 上级节点为 `ROOT`

5. 本地模板与策略文件校验：
   - 结果：
     - `.github/ISSUE_TEMPLATE/00-wbs-parent-node.md` 存在
     - `.github/ISSUE_TEMPLATE/01-wbs-execution-node.md` 存在
     - `.github/ISSUE_TEMPLATE/config.yml` 存在
     - `02-当前工程Codex工作策略.md` 已同步最新策略

## 3. 风险与限制

- `AddSubIssue` 权限不足，无法启用 GitHub 原生父子 Issue
- 标签维护权限不足，无法创建治理标签体系

## 4. 结论

- 本轮重构目标已达成，状态建议：`待人工验收`
