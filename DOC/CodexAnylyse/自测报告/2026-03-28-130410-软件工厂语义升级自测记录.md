# 2026-03-28 13:04:10 软件工厂语义升级自测记录

## 1. 本轮目标

将现有浏览器原型从“场景构建台”语义升级为更贴近需求的“软件工厂”语义，并保持后端链路可用。

## 2. 本轮产物

- `src/sampleData/workbenchBootstrap.js`
- `public/index.html`
- `public/app.js`
- `public/styles.css`
- `tests/integration/httpApi.test.js`
- `DOC/CodexAnylyse/验收清单/2026-03-28-130410-人工验收清单.md`

## 3. 自测项

### 3.1 聚焦测试

已执行：

- `npm test -- --test-name-pattern="GET / returns the scenario builder shell|GET /api/workbench/bootstrap returns fixed sample data"`

结果：

- 语义升级前失败
- 完成补丁后通过

### 3.2 全量测试

已执行：

- `npm test`

结果：

- 10 个测试全部通过

### 3.3 静态检查

已执行：

- `npm run lint`

结果：

- 通过，无 ESLint 报错

## 4. 本轮实现要点

- bootstrap 数据新增：
  - `repositoryMeta`
  - `stateCollections`
  - `factoryCapabilities`
  - `softwareIntentTemplate`
- 首页三栏语义调整为：
  - `行业/领域知识仓库`
  - `软件工厂`
  - `定制软件定义区`
- 右栏输出优先显示 `软件定义` 视角，并把 Scenario 包装进更高层的软件定义结构
- 中栏新增 `构建能力单元` 选择，并把选择结果传入场景组合输入

## 5. 当前限制

- 当前左栏知识仓库仍是固定样本，不是动态浏览真实仓库
- 当前“软件定义”仍以结构化 JSON 呈现，尚未扩展为更丰富的图形化定义视图
- 当前状态集合仅作为仓库切面展示，尚未提供状态检索与回写追踪界面

## 6. 结论

本轮代码已达到“可浏览、可交互、术语升级完成、链路保持可用”的目标，状态为：

- 待人工验收
