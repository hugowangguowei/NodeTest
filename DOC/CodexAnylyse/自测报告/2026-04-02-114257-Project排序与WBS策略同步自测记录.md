# Project 排序与 WBS 策略同步自测记录

时间：2026-04-02 11:42:57

## 1. 自测目标

确认以下变更已落地：

1. `hugowangguowei/projects/1` 与 `wgwtest/projects/2` 的 WBS 顺序一致
2. `L0(#7)` 下的 L1 顺序固定为 `P1 -> P2 -> P3 -> P4`
3. 本地策略文档已写入“Issue Tree 真源 + Project 镜像 + WBS 顺序强约束”

## 2. 执行命令与结果

1. `gh project item-list 1 --owner hugowangguowei --format json | jq -r '.items[].content.number'`
   - 结果：`7 8 1 2 3 5 9 10 11 4 12 13 14 6 15 16 17`
2. `gh project item-list 2 --owner wgwtest --format json | jq -r '.items[].content.number'`
   - 结果：`7 8 1 2 3 5 9 10 11 4 12 13 14 6 15 16 17`
3. `gh api graphql` 查询 `issue #7 subIssues`
   - 结果：`#8 #5 #4 #6`，即 `P1 -> P2 -> P3 -> P4`
4. `git diff -- DOC/CodexAnylyse/...`
   - 结果：策略文档与计划入口文档已更新为主/镜像 Tracker 和 WBS 强约束口径

## 3. 自测结论

本轮目标全部通过，当前可进入人工验收。
